fn main() {
    let x = 2.0;
    let y: f32 = 3.0;

    let t = true;
    let f: bool = false;

    let tup: (i32, f64, u8) = (500, 6.4, 1);
    let (x, y, z) = tup;

    println!("the value of y is: {y}");
}
